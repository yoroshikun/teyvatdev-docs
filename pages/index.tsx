import Layout from '../components/Layout';

export const Home = (): React.ReactNode => {
  return (
    <Layout title="Home" description="Test">
      This is the index
    </Layout>
  );
};

export default Home;
